services:
  film-rating:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: film-rating
    ports:
      - '3000:3000'
    volumes:
      - ./data:/app/data
    restart: unless-stopped
